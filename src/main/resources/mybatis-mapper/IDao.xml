<?xml version="1.0" encoding="UTF-8"?>

<!DOCTYPE mapper
   PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
   "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
   
<mapper namespace="com.studycafe.prac.dao.TodayTicketDao">

	<insert id="regist">
		INSERT INTO scseatTBL(seatNo, userId, ticketName, selectedDate, selectedTime, TEMPUSINGNO)
		 VALUES(#{param1}, #{param2}, #{param3}, #{param4},#{param5},TEMPUN_seq.nextval) 
	</insert>


	<select id="registTodayConfirm" resultType="com.studycafe.prac.dto.seatDto">
		select * from scseatTBL order by tempUsingNo desc
	
	</select>
</mapper>
